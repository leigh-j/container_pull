all:
  vars:
    ansible_host: localhost
    ansible_connection: local
  children:
    ocp4:
      hosts:
        localhost:
        ocp:
    ocp3:
      hosts:
        localhost:
        ocp:
        

